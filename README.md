# Prometheus Blackbox Exporter

This is a hack to get `blackbox_exporter` into a container using `pack` so it can be deployed on Toolforge.

Runtime configuration is managed by `scripts/entrypoint.py`
